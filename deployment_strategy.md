# Стратегия деплоя и разработки

## 🎯 Цель
Безопасная разработка проекта с сохранением данных в продакшене на Hostinger, при этом возможность тестирования локально.

## 📁 Структура проекта

### Локальная разработка (тестовый режим)
```
enoterra_erp/
├── src/                    # React приложение
├── server/                 # Express сервер
│   ├── index.js           # Основной сервер
│   ├── enoterra_erp.db   # Локальная БД (тестовая)
│   └── uploads/           # Загрузки
├── database_*.mjs         # Утилиты БД
├── package.json           # Зависимости
└── .env                   # Локальные настройки
```

### Продакшен на Hostinger
```
public_html/
├── dist/                   # Собранное React приложение
├── server/                 # Express сервер
│   ├── index.js           # Основной сервер
│   ├── enoterra_erp.db   # Продакшен БД (с данными)
│   └── uploads/           # Загрузки
└── .env                   # Продакшен настройки
```

## 🔄 Рабочий процесс

### 1. Локальная разработка
```bash
# Запуск в тестовом режиме
npm run dev              # React dev server
cd server && npm run dev # Express dev server
```

### 2. Тестирование изменений
```bash
# Проверка структуры БД
node database_check.mjs

# Тестирование новых функций
npm run build
cd server && npm start
```

### 3. Деплой на Hostinger
```bash
# Сборка для продакшена
npm run build

# Загрузка файлов на сервер
# - dist/ → public_html/dist/
# - server/ → public_html/server/
# - .env → public_html/.env
```

## 🛡️ Защита данных

### База данных
- **Локальная БД**: `enoterra_erp.db` (тестовая, можно очищать)
- **Продакшен БД**: `public_html/server/enoterra_erp.db` (защищенная)

### Миграции
```bash
# Локальные миграции (тестирование)
node database_migrations.mjs

# Продакшен миграции (осторожно!)
# Только после тестирования локально
```

### Резервное копирование
```bash
# Автоматическое резервное копирование продакшен БД
# Перед каждым деплоем
```

## 📋 Скрипты для деплоя

### 1. Скрипт подготовки к деплою
```bash
node prepare_deployment.mjs
```

### 2. Скрипт сборки для продакшена
```bash
npm run build:prod
```

### 3. Скрипт проверки совместимости
```bash
node check_compatibility.mjs
```

### 4. Скрипт безопасного деплоя
```bash
node safe_deploy.mjs
```

## 🔧 Конфигурация

### Локальная разработка (.env)
```env
NODE_ENV=development
PORT=3001
DB_PATH=./enoterra_erp.db
VITE_API_URL=http://localhost:3001
```

### Продакшен (.env)
```env
NODE_ENV=production
PORT=3001
DB_PATH=./enoterra_erp.db
VITE_API_URL=https://yourdomain.com
```

## 🚀 Процесс деплоя

### Шаг 1: Локальная разработка
1. Разрабатываете новые функции локально
2. Тестируете на локальной БД
3. Проверяете совместимость

### Шаг 2: Подготовка к деплою
1. Создаете резервную копию продакшен БД
2. Тестируете миграции на копии
3. Собираете приложение

### Шаг 3: Деплой
1. Загружаете файлы на Hostinger
2. Запускаете миграции (если нужно)
3. Перезапускаете сервер

### Шаг 4: Проверка
1. Проверяете работу приложения
2. Проверяете сохранность данных
3. Мониторите логи

## ⚠️ Важные правила

### ✅ Что можно делать
- Разрабатывать новые функции локально
- Тестировать на локальной БД
- Добавлять новые таблицы/колонки
- Изменять UI/UX

### ❌ Что нельзя делать
- Удалять существующие данные
- Изменять структуру существующих таблиц без миграций
- Деплоить без тестирования
- Изменять продакшен БД напрямую

## 🔍 Мониторинг

### Логи сервера
```bash
# Локальные логи
cd server && npm run dev

# Продакшен логи (на Hostinger)
tail -f server/logs/app.log
```

### Проверка БД
```bash
# Локальная проверка
node database_check.mjs

# Продакшен проверка (осторожно!)
# node database_check.mjs
```

## 📦 Автоматизация

### Скрипт деплоя
```bash
# deploy.sh
#!/bin/bash
echo "🚀 Starting deployment..."

# 1. Backup production database
echo "📦 Creating backup..."
cp public_html/server/enoterra_erp.db backup_$(date +%Y%m%d_%H%M%S).db

# 2. Build application
echo "🔨 Building application..."
npm run build

# 3. Upload files
echo "📤 Uploading files..."
# rsync или scp команды

# 4. Run migrations (if needed)
echo "🔄 Running migrations..."
# node database_migrations.mjs

# 5. Restart server
echo "🔄 Restarting server..."
# pm2 restart или systemctl restart

echo "✅ Deployment completed!"
```

## 🎯 Преимущества этой стратегии

1. **Безопасность данных**: Продакшен БД защищена
2. **Гибкость разработки**: Можно экспериментировать локально
3. **Простота деплоя**: Автоматизированный процесс
4. **Мониторинг**: Контроль за изменениями
5. **Откат**: Возможность вернуться к предыдущей версии

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Сравните структуру БД локально и на сервере
3. Восстановите из резервной копии при необходимости
4. Проверьте совместимость версий 